let cookie = prompt("agree to accept cookie");

if (cookie.toLowerCase() === "yes") {
  // toLowerCase() is used to convert the user input to lowercase before comparing it with "yes".
  console.log("u're in a trap, my boy");
}
